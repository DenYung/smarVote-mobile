import React from 'react'
import { ActivityIndicator } from 'react-native'

function Spinner() {
    return (
        <ActivityIndicator size="large" color="#30b2e6" />
    )
}

export default Spinner;
